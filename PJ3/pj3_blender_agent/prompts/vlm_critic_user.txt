I generated a 3D model of: {description}

The images show 4 rendered views: front, side, top, and isometric (45°).

Identify all structural and geometric problems visible in the images. Describe what is visually wrong — do NOT suggest code fixes or specific values.

Output as JSON array of issues:
